# Copy this file to .env and edit as needed.

# When true (default), all logic runs on deterministic, rule-based mock data.
# No API key needed, no network calls.
MOCK_MODE=true

# Only required when MOCK_MODE=false. Get a key from https://console.anthropic.com/
ANTHROPIC_API_KEY=
